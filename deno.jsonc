{
  "tasks": {
    "test": "deno test --allow-read --allow-net",
    "fasttest": "deno test --allow-read --allow-net --ignore=denops/app/preview_server_test.ts --watch",
    "servertest": "deno test --allow-read --allow-net denops/app/preview_server_test.ts --watch",
    "check": "deno lint && deno fmt && deno task test && git status"
  }
}
